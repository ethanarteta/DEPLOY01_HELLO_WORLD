pipeline {
  agent any

  stages {
    stage ('Build') {
      steps {
        sh "echo Run a command"
     }
   }
    stage ('test') {
      steps {
        sh "echo Run a command"
      }
    }
    stage('Deploy') {
            steps {
                sh 'echo Deploying the application'
            }
        }
    }
}
